# 伞项目 qwen3.8-27B 归档 2026-08-26（逐窗口监控 + qwen Hermes 计价接入）

> 本次固化两块运维能力：① qwen 逐窗口用量**常驻监控**；② Hermes token **计价接入**（配 deepseek-v4-flash 峰谷静态价）。伞 git tag `archive-20260826`。

## 一、新增子项：`monitor/`（逐窗口用量监控 + 计价配置）

### 1. 逐窗口用量常驻监控
- **数据源**：`~/.hermes/profiles/qwenthink/logs/agent.log`。Hermes 每窗口是独立进程，每行 API 调用带 `[session_id]`(=窗口标识)，含 `in/out/total`(token) 与 `latency`(秒)。同一 session 前缀 = 一个窗口生命周期。
- **脚本** `monitor/qwen_window_monitor.py`：按窗口聚合 calls/input/output/total/latency + ●活跃标记。
  `python qwen_window_monitor.py`(当日) / `--dump`(全历史) / `--append`(追加快照到 monitor.log)。
- **常驻**：Windows 计划任务 `qwen-window-monitor` 每 10min 跑 `--append`。全程只读 agent.log，不打扰运行窗口。

### 2. 计价接入（Hermes 源码）
- **背景**：Hermes 计价来源只有 (a)内置表 `_OFFICIAL_DOCS_PRICING` / (b)`/models` 的 `pricing` 字段。clone4 llama-server `/models` 不返 pricing → qwen 原报「未配置计价」。
- **架构限制**：`PricingEntry` 仅 input/output/cache_read/cache_write 四价，成本纯乘法，**无峰/谷/时间维度** → 峰谷无法表达，只能静态一价。
- **已做**：改 `E:\ObsidianHouse\hermes-agent-main\agent\usage_pricing.py` 内置表，加 `("qwenthink","qwen3.8-27b")`=**deepseek-v4-flash 高峰价**（备份 `.bak-20260826`）：
  - input $0.44（缓存未命中·峰）
  - cache_read $0.014（缓存命中·峰）
  - output $1.32（输出·峰）
  - source_url=https://api-docs.deepseek.com/quick_start/pricing，pricing_version=`deepseek-pricing-2026-08-flash-peak`
- **取价来源**（deepseek 官网「模型 & 价格」2026-08，USD/百万tokens）：V4-Flash 缓存未命中 峰$0.44/谷$0.22、缓存命中 峰$0.014/谷$0.007、输出 峰$1.32/谷$0.66。V4-Pro=3×。
- **生效范围**：改源码 → **新 qwen 窗口生效**；运行中旧窗口进程持旧模块，需重启该窗口才显示成本。
- **切换**：改 usage_pricing.py 三个 Decimal 即可（谷价 0.22/0.007/0.66 或 V4-Pro 峰 1.32/0.044/3.96）。

## 二、实例状态（2026-08-26，同 08-25）
| 实例 | 卡/sm | 角色 | 状态 |
|---|---|---|---|
| **clone4** | 4080SUPER/sm89 | Hermes 思考(budget2000) | **在线**（2 个 qwen 窗口连此） |
| clone2 | 4080/sm89 | Hermes 思考 | 离线(带卡可起) |
| clone1 | 4080SUPER/sm89 | Hermes 思考 | 离线(带卡可起) |
| clone3 | 4080/sm89 | Hermes 思考 | 离线(带卡可起) |
| autodl-5090 | 5090/sm120 | codex-q38(off) | 离线(需开机) |
> 实时并发：clone4 `-np 1` 单槽 → 多窗口错峰可用，同时请求会排队（latency 被拉长，见 agent.log 交错时间轴）。

## 三、工具坑（吸取）
- `git add -A` 误把 `branches/` 两个独立子仓库当 gitlink(mode 160000) 加进伞 → 已 `-f` 移除 + 加进 `.gitignore`。伞永远不跟踪 branches 子仓。
- 监控运行产物 `monitor/*.log` 已 gitignore。

## 四、待办（延续）
- **clone1/clone3 下次开机同步 budget2000**（仍是 8.23 无 budget 旧快照）：`scp watchdog-vps/start_clone89_std.sh → /root/autodl-tmp/start_clone89.sh`。

## 归属 / 续接
```
qwen3.8-27B（伞，archive-20260826）
├── monitor/                      ★新：逐窗口监控 + 计价配置(README 含峰谷价表/切换)
├── branches/qwen38-5090-deploy/  子项目①
├── branches/qwen38-vgpu-deploy/  子项目②
├── watchdog-vps/                 VPS 看门狗(4台)
├── tools/skills/qwen38-clone-server/  一键克隆skill快照
└── ARCHIVE-2026-08-26.md         本归档
```
说「按交接提示继续」→ 伞 README + 记忆 `qwen38-clone-switch-resume-handoff` + `monitor/README.md`。