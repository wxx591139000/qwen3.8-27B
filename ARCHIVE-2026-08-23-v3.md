# 伞项目 qwen3.8-27B 归档 2026-08-23-v3（决策：codex+qwen 暂停 / 主力改 Hermes）

> 在 v1.1/v2 基础上，本次归档固化一次**技术决策**与 3 台 clone 齐整的状态。伞 git tag `archive-20260823-v3`。

## 本次决策（核心）
**codex + qwen（思考机）组合暂停启用。**
- **原因**：codex 走 `/v1/responses` 且强制 `reasoning effort: high`；服务器 `--reasoning on` 时 responses 触发无界思考 → 超时/空响应 → codex 连不上。已定位为根因（responses X reasoning 冲突，见 skill 事实库 §codex）。
- **动作**：codex provider（`qwen38clone1/2/3`）+ PS 别名（`codex-clone1/2/3-free`）**保留未删**，标记暂停；`--reasoning off` 的 5090 生产档 `codex-q38` 不受影响，仍有效。
- **当前主力开发路径**：**Hermes + qwen 思考**（✔ 兼容：Hermes 走 chat 端点，服务器 on 思考正常）。`hermes-qwen` 指 clone2 公网。

## 本次状态整备（相对 v2 增量）
- **3 台 clone 齐成 clone2 状态**：clone2(源)→clone1、clone2→clone3(原 `autodl-qwen-vgpu` 改名) 经 AutoDL 跨实例拷贝系统盘+数据盘，均带完整 qwen 部署（Q4_K_M+mtp+mmproj+修复模板+6key+build89）。
- **VPS 看门狗扩到 3 台巡检**（多机 TARGETS：clone1 westc:46949 / clone2 westc:19407 / clone3 westd:31102，均 `start_clone89.sh` Hermes思考档）；README/脚本同步。
- **skill 新增第⑧步「VPS 看门狗（默认自动配）」**：未来任何新克隆 qwen 机，默认自动加进 VPS TARGETS 拉起（用户偏好已固化入记忆）。
- codex 新增 `qwen38clone3` provider + `codex-clone3-free` + ssh 别名 `autodl-qwen-clone3`（只增不改）。

## 验证锚点
- clone2 在线：`/health` ok（本机+公网），RTX4080 27.3GB，看门狗 22:13:55 拉起→22:14:06 恢复，**约 11s 全程零人工**。
- clone1/clone3 带卡开机即被看门狗拉起（TARGETS 已含）。

## 归属
```
qwen3.8-27B（伞，archive-20260823-v3）
├── branches/qwen38-5090-deploy   子项目①：5090生产档（★源/主；codex-q38 off 仍可用）
├── branches/qwen38-vgpu-deploy   子项目②：vGPU平价档
├── tools/skills/qwen38-clone-server/   一键克隆skill快照（含步⑧ VPS看门狗默认自动配）
└── watchdog-vps/                 辅助子项：VPS看门狗（多机3台巡检）
```

## 续接
说「按交接提示继续」→ `qwen38-clone-switch-resume-handoff` 记忆 + 伞 README + 子项目① `docs/接交接.md`。