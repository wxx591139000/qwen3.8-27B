# 伞项目 qwen3.8-27B 归档 2026-08-24（决策：主力档 `--reasoning-budget 2000` 落定）

> 本次固化一次**用数据支撑的技术决策**：给 3 台 clone 的 llama-server start 脚本统一加 `--reasoning-budget 2000` 作为**主力思考护栏**，并沉淀三档（无界/500/2000）能力对比的完整结论。伞 git tag `archive-20260824`。

## 核心决策（本次）
**主力档 = `--reasoning-budget 2000`**（B 方案落定）。based on 实测三档能力对比 + 业界参照（见下）。clone2 已改（脚本+运行进程），clone1/clone3 待下次开机同步（标准档已备本地 `watchdog-vps/start_clone89_std.sh`）。

## 背景与动机
- **症状**：无思考预算时，Hermes 压缩假死、图片首轮超时（1×1 图 63s / APITimeoutError）——根因是 `--reasoning on` **无界思考**塞爆客户端等待窗口、GPU 为客户端已放弃的请求空转烧卡。
- **方案**：给思考加上限做护栏（B），而非靠"等机空闲"。budget 用多少不伤质量 → 跑三档对比验证。

## ★Budget 三档能力对比（aime 6题 + coding 8题，`bench_capability.py`）
**首轮（单次，波动大）+ 复核（关键档 aime 各补 2 轮）**

| 档 | aime（数学）| coding（代码）| 判定 |
|---|---|---|---|
| **无界** | 首轮100%(运气) → 复核 83%/83%，均值 ~89% | **37.5%**（5题挂）| ❌ 假天花板 |
| **500** | 83% | 50% | ⚠️ 劣档 |
| **2000** | 首轮67%(运气差) → 复核 **100%/100%**，均值 ~89% | **75%**（最优）| ✅ 甜区 |

**三条铁结论**
1. **2000 不伤质量还更稳**：aime 三轮均值 ~89%（**≥无界**，复核连续满分），coding 75% **吊打无界 37.5%**。
2. **无界=假天花板**：看似 aime 高，实为单次运气（复核掉到 83）；且无界思考**吃满 max_tokens(4096) 挤占代码输出** → coding 惨败。"无界=质量上限"被证伪。
3. **500 是劣档**：aime 83 / coding 50 双低于 2000 —— 护栏并非越小越好，思考压太紧反损。

**景气：首轮两档的"异常"都是小样本反向噪声**（无界 100 幸运、2000 67 倒霉），复核抹平并揭示 2000 ≥ 无界。

## 业界参照（印证 2000 是默认中档）
- **Claude** extended thinking：`budget_tokens` 设「略大于实际需要思考量」，常见 3200–16000+。
- **OpenAI**：`reasoning_effort` low/medium/high，按任务选档，默认 medium。
- **DeepSeek**：无独立 thinking-budget，靠 `max_tokens` 兜底（"别卡思考"的教训被 coding 数据反证卡到合理档更优）。
- 2000 token ≈ Claude 3200 基线 / OpenAI medium —— 正是业界默认中档，非偏小。

## 状态整备（相对 v3 增量）
- **clone2**：`start_clone89.sh` 已加 `--reasoning-budget 2000`（脚本+进程 PID 27593 均生效，备份 `start_clone89.sh.bak-nobudget`）。VPS 看门狗拉起时即带 budget（watchdog 调每台各自 start_clone89.sh，脚本带即生效）。
- **本地标准档**：`watchdog-vps/start_clone89_std.sh`（budget 2000，供 clone1/3 与新克隆参照）。
- **clone1/clone3**（关机）：**待办**——下次开机时 `scp watchdog-vps/start_clone89_std.sh /root/autodl-tmp/start_clone89.sh` 同步 budget2000（她们仍是 8.23 无 budget 旧快照）。

## 验证锚点
- 三档对比全程 clone2 在线：aime+coding 各档跑通、复核稳定（2000 aime 2×100%、无界 2×83%）。
- 最终 clone2 恢复 budget2000 标准档，health ok。

## 归属 / 续接
```
qwen3.8-27B（伞，archive-20260824）
├── branches/qwen38-5090-deploy/   子项目①（含 docs/benchmarks/capability 基准套件）
├── branches/qwen38-vgpu-deploy/   子项目②
├── watchdog-vps/start_clone89_std.sh   ★budget2000 标准启动档
└── ARCHIVE-2026-08-24.md          本归档
```
说「按交接提示继续」→ `qwen38-clone-switch-resume-handoff` 记忆 + 伞 README + 待办。