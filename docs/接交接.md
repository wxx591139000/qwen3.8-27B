# qwen3.8-27B 接交接（归档 2026-09-16 · archive-20260916-v2）

> 说「**按交接提示继续**」= 续接。先读本文件 + memory `[[hermes-qwen-handover-2026-09-16]]`，对齐锚点直接干。

## 项目现状
- **本地伞项目**：`E:\myClaudCodeWorkspace\qwen3.8-27B`（master `13da0d2`，Git remote `github.com/wxx591139000/qwen3.8-27B.git`，tags `archive-20260916` + `archive-20260916-v2`，已 push）
- **版本**：本次 = Hermes qwen 自动切换 + clone5(westb) 接入 + PowerShell 404 根治
- **总交接文档**：`docs/克隆服务器交接清单.md`（克隆 qwen 服务器全部工作的总入口：5步+三处地址登记+Hermes两种模式+看门狗+验证清单+机况表+硬约束）
- **切换脚本**：`monitor/hermes_qwen_switch.py`（`--scan/--switch/--set/--restore`）

## Hermes qwen 入口（★硬坑）
- 命令 `hermes-qwen` 定义在 **PowerShell profile** `C:\Users\Dancing\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1`（**非 bashrc**）
- `hermes-qwen` = 自动探测在线 clone → 回写 `qwenthink` profile base_url → 启动 `hermes -p qwenthink chat`
- `--scan` 只看在线 / `--set cloneX` 强制 / `--restore` 恢复；改后须**新开 PS 窗口**生效
- ⚠️ 曾踩坑：加 bashrc → PS 仍用旧函数透传 `--scan` → `unrecognized arguments` + base_url 未切 → **404**

## 机况速查（2026-09-16）
| 机 | SSH别名 | 公网 base_url | 状态 |
|---|---|---|---|
| clone5(★) | autodl-qwen-clone5 westb:27190 | u1068217-tvka-e5d7d4be.westb:8443/v1 | 在线已校准，看门狗可拉起 |
| clone4 | autodl-qwen-clone4 westd:18574 | u1068217-x588-77ebd208.westd:8443/v1 | 关机 |
| clone2 | autodl-qwen-clone2 westc:19407 | u1068217-b9c1-805a0fa4.westc:8443/v1 | 关机 |
| clone1/3 | autodl-qwen-clone1/3 | （公网待补） | 关机 |

## 遗留待办
- [ ] clone1/clone3 公网 URL 补进 switch 脚本 TARGETS
- [x] ~~真机无参跑 `hermes-qwen` 端到端验证交互启动~~ —— 2026-09-16 已验：PS `. $PROFILE` 后 `hermes-qwen` 自动切 clone5 并进 hermes，404 根治

> ✅ **全链路已验证**（2026-09-16）：clone5 在线 → PowerShell `hermes-qwen --scan` 见 clone5(200)、`hermes-qwen` 自动切到 clone5(westb:e5d7d4be) 并启动，不再 404。三环（切换脚本/PS函数/看门狗）全通。

## 硬约束
- 用户用 **PowerShell**；hermes-qwen 改 PS profile（UTF-8），新开窗口生效
- 克隆 qwen 机必做**三处地址登记**（switch脚本 / VPS看门狗 / ssh config），漏则认不出新机
- 不动 5090 生产档；一台机只跑一种角色（思考 on / codex off 互斥）
- AutoDL 按小时计费，用完关机

## 续接路径
说「**按交接提示继续**」→ 读 `docs/克隆服务器交接清单.md` + memory `[[hermes-qwen-handover-2026-09-16]]`，对齐 tag/机况/待办直接干。