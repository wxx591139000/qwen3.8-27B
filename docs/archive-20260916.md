# 归档 archive-20260916

**日期**：2026-09-16
**项目**：qwen3.8-27B（Qwen3.8-27B 克隆生态 + Hermes 接入）
**Git**：master `3a10dd1` · tag `archive-20260916` · 已 push

## 本次归档变更
1. **新增 `monitor/hermes_qwen_switch.py`** —— Hermes qwen「开哪台连哪台」自动切换器：
   - 探测各 clone 公网 → 选在线(200)机 → 备份 → 回写 `qwenthink` profile base_url（只改一行）
   - `--scan` 只看在线 / `--switch` 自动切 / `--set cloneX` 强制 / `--restore` 恢复
   - 安全：已关机机返回 AutoDL nginx 404（快速非超时），非 200 判离线，不误切
2. **新增 `docs/克隆服务器交接清单.md`** —— 克隆 qwen 服务器总入口文档（5步流程、三处地址登记、Hermes 两种模式、看门狗、验证清单、机况表、硬约束）
3. **`watchdog-vps/qwen_watchdog.sh`** —— 同步为 VPS 权威版：TARGETS 补 clone4，clone5 改为 westb:27190（共 5 台）
4. **`tools/skills/qwen38-clone-server/SKILL.md`** —— 流程总览开头加指针指向交接清单

## 系统级变更（不在本 git 仓库，靠交接文档+memory 记录）
- **clone5 新接入**：`autodl-qwen-clone5` → westb:27190；VPS 看门狗 TARGETS 加 clone5；llama-server `--reasoning on --reasoning-budget 2000` 已起、health ok、公网 200(已校准)；VPS→clone5 免密通
- **PowerShell profile** 的 `hermes-qwen` 修正为自动切换版（404 根治；详见交接清单⚠️硬坑）
- ssh config 新增 clone5 别名

## 待办
- [ ] clone1/clone3 公网 URL 补进 switch 脚本 TARGETS

## ✅ 追加验证（archive-20260916-v3）
**Hermes qwen 全链路端到端实测通过**（PowerShell）：
- `. $PROFILE` 重载后 `hermes-qwen --scan` 正确探到 clone5(200)，其他关机
- `hermes-qwen` 自动切到 clone5(westb:e5d7d4be) → 启动 hermes → **不再 404**
- 关键点：已是新版 PS 函数（补 BOM 中文正常）；旧窗口缓存旧函数 → 改 profile 后须 `. $PROFILE` 或新开窗口
- 三环（切换脚本 / PS 函数 / VPS 看门狗）全通

## 核心文档索引
- 交接总入口：`docs/克隆服务器交接清单.md`
- power 脚本：`monitor/hermes_qwen_switch.py`
- SKILL：`tools/skills/qwen38-clone-server/SKILL.md`
- 看门狗：`watchdog-vps/qwen_watchdog.sh`