# 伞项目 qwen3.8-27B 归档 2026-08-23-v2（新增辅助子项：VPS看门狗）

> 在 v1.1 重构归档(`archive-20260823`)基础上，本次增量归档把「阿里云 VPS 自动拉起 qwen 服务」作为**辅助子项**并入伞范围。伞 git tag `archive-20260823-v2`。

## 本次动作
- **新增辅助子项 `watchdog-vps/`**（伞仓库顶层目录）：
  - `qwen_watchdog.sh`  — 长驻探测循环（每60s：机器在线∧llama down → SSH 拉起 → 等 health）
  - `qwen-watchdog.service` — VPS systemd unit（`Restart=always`、`enabled` 自启）
  - `README.md` — 架构、部署、复刻到其他 qwen 服务器、运维。
  - 文件为从 VPS 实机 `vps-aliyun`(`/opt/qwen-watchdog/`、`/etc/systemd/system/`) 原样取回的**版本化存档**；活实例仍在 VPS 上跑。
- **伞 README 升 v1.2**：结构树加入 `watchdog-vps` 辅助子项。
- **事实库同步**：`tools/skills/qwen38-clone-server/references/qwen38_facts.md` 增「开机自启」小节（AutoDL 无 systemd/cron、自定义服务仅端口映射 → 唯一可靠通道=VPS 看门狗）。

## 为什么看门狗站得住
- AutoDL 容器 PID1=`/init/boot/boot.sh`：systemd offline、无 cron、自带 supervisord 配置在 `/init/` 临时区重启丢 → **机器内无持久自启通道**。
- 控制台「自定义服务」实测**只做端口映射（6006/6008→公网），不能执行开机命令**。
- 因此用常开的阿里云 VPS（正规 systemd）作外部监护人：不只开机自启，白天 llama-server 崩溃也自愈。

## 当前部署（clone2 实机已验证在线）
| | |
|---|---|
| VPS | `vps-aliyun` (101.200.227.65)，service `qwen-watchdog` active+enabled |
| 钥匙 | VPS 独享 `id_watchdog`(ed25519)，已授权到 clone2 `/root/.ssh/authorized_keys`（不共享本机 key） |
| 探测目标 | clone2 `connect.westc.seetacloud.com:19407`，启动 `start_clone89.sh`（`--reasoning on` Hermes 档） |
| 复刻 | 改脚本顶部 `PORT/HOST/START` 三处 + 配独享 key 即可并行多机 |

## 归属
```
qwen3.8-27B（伞，tag archive-20260823-v2）
├── branches/qwen38-5090-deploy   子项目①：5090 生产档（★源/主）
├── branches/qwen38-vgpu-deploy   子项目②：vGPU 平价档
├── tools/skills/qwen38-clone-server/   一键克隆skill 快照
└── watchdog-vps/                 ★辅助子项：VPS 看门狗（本次）
```

## 续接
说「按交接提示继续」→ `qwen38-clone-switch-resume-handoff` 记忆 + 伞 README + 子项目① `docs/接交接.md`。