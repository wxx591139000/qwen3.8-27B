# 伞项目 qwen3.8-27B 归档 2026-08-26 v2（成本核算两档 + 每日自动任务 + deepseek-pricing 收录）

> 本次固化：① 峰谷成本核算脚本 `monitor/qwen_cost.py`（**默认两档 Flash+Pro 每日表格**，纯本地零 token）；② 每日 8点/17点 自动任务；③ 收录 Hermes `deepseek-pricing` skill 到 `docs/`。伞 git tag `archive-20260826-v2`。

## 一、新增：峰谷成本核算 `monitor/qwen_cost.py`
- **作用**：qwenthink profile（clone4）每日 DeepSeek 等价成本核算，按 `deepseek-pricing` skill §4 口径。
- **零 token**：纯 Python 读 `~/.hermes/profiles/qwenthink/state.db`（只读 SQLite `mode=ro`），不调任何模型；金额统一带 ¥/$ 单位；日历对齐北京时间。
- **默认两档**：`Flash` / `Pro` 各列 **混合(实际)＋全峰(上限)**（谷=峰×0.5 不单列），每日期一行 + 合计行。
- **计费公式**：`cost_peak=新输入×未命中峰价+缓存×命中峰价+输出×输出峰价`；`f`=落在峰窗口(北京工作日09-12/14-18)的 assistant 消息占比；`cost_mix=f×peak+(1-f)×off`。
- `--day YYYY-MM-DD`/`--detail` 分会话明细(两档混合)；`--usd` 美元；`--since N`。
- **实测**(2026-08-26 全量)：缓存命中 ~89%，混合 Flash ¥31.27 / Pro ¥93.80（与 skill 基准 ¥26/¥80 区间相符，含今日新增）。

## 二、每日自动任务（Windows 计划任务）
| 任务名 | 触发 | 命令 | 落盘 |
|---|---|---|---|
| `qwen-cost-0800` | 每日 08:00 | `python qwen_cost.py --since 1 --report` | `monitor/daily_cost.log` |
| `qwen-cost-1700` | 每日 17:00 | 同上 | 同上（追加） |
| `qwen-window-monitor` | 每 10 分钟 | `python qwen_window_monitor.py --append` | `monitor/monitor.log` |
> 建任务坑：MSYS bash 传装 `//ST 08:00` 会被转义破坏 → 用 python `subprocess` 调 `schtasks`(list 参数) 解决。`*.log` 已在 .gitignore。

## 三、收录 Hermes skill → `docs/deepseek-pricing/SKILL.md`
- 来源 `~/.hermes/profiles/qwenthink/skills/research/deepseek-pricing/`（用户 Hermes 侧自建，research 分类）。
- 内容：DeepSeek 官方峰谷价（中/美元，V4-Flash/Pro/Vision）、图像 token 规则、Hermes state.db 成本核算方法、陷阱。原样收录（含 frontmatter）。

## 四、状态
clone4 在线（2 qwen 窗口）、`-np 1`；clone1/2/3/5090 离线。

## 五、待办（延续）
- **clone1/clone3 开机同步 budget2000**（仍在）。
- qwen 窗口显示成本需重启（Hermes 计价改的是源码，运行中进程不重载）。

## 归属 / 续接
```
qwen3.8-27B（伞，archive-20260826-v2）
├── monitor/                      逐窗口监控 + 两档峰谷成本核算 + 每日任务 + README
├── docs/deepseek-pricing/       ★Hermes skill 收录
├── branches/{qwen38-5090-deploy,qwen38-vgpu-deploy}   独立子仓
├── watchdog-vps/                VPS 看门狗(4台)
└── ARCHIVE-2026-08-26-v2.md     本归档
```
说「按交接提示继续」→ 伞 README + 记忆 `qwen38-clone-switch-resume-handoff` + `monitor/README.md`。